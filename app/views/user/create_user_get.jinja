<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Create User</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        body {
            background: linear-gradient(120deg, #f8fafc 0%, #e0e7ff 100%);
            font-family: 'Poppins', sans-serif;
            min-height: 100vh;
        }
        .create-user-card {
            border-radius: 2.5rem;
            box-shadow: 0 12px 40px 0 rgba(31, 38, 135, 0.18);
            background: rgba(255,255,255,0.98);
            border: none;
            margin-top: 60px;
        }
        .create-user-title {
            font-family: 'Poppins', sans-serif;
            letter-spacing: 1.5px;
            font-size: 2rem;
            color: #4f46e5;
            margin-bottom: 1.5rem;
            text-shadow: 0 2px 8px rgba(99,102,241,0.07);
        }
        .form-label {
            color: #6366f1;
            font-size: 1.15rem;
            font-weight: 600;
        }
        .form-control {
            border-radius: 2rem;
            padding: 0.75rem 1.5rem;
            border: 1.5px solid #e0e7ff;
            background: #f4f6fb;
            font-size: 1.05rem;
        }
        .form-control:focus {
            border-color: #6366f1;
            box-shadow: 0 0 0 0.18rem rgba(99,102,241,.13);
            background: #fff;
        }
        .btn-primary {
            background: linear-gradient(90deg, #6366f1 0%, #818cf8 100%);
            color: #fff;
            border-radius: 2rem;
            font-weight: 600;
            letter-spacing: 1px;
            font-size: 1.08rem;
            box-shadow: 0 2px 12px 0 rgba(99,102,241,0.10);
            border: none;
        }
        .btn-primary:hover, .btn-primary:focus {
            background: linear-gradient(90deg, #818cf8 0%, #6366f1 100%);
            color: #fff;
        }
    </style>
</head>
<body>
    <div class="container d-flex align-items-center justify-content-center min-vh-100">
        <div class="card create-user-card shadow-lg p-4" style="min-width: 350px; max-width: 450px; width: 100%;">
            <div class="card-body">
                <h2 class="create-user-title text-center fw-bold">Create New User</h2>
                {% if error %}
                    <div class="alert alert-danger text-center" role="alert">
                        {{ error }}
                    </div>
                {% endif %}
                {% if success %}
                    <div class="alert alert-success text-center" role="alert">
                        {{ success }}
                    </div>
                {% endif %}
                <form method="post" action="/create_user/">
                    <div class="mb-3">
                        <label for="username" class="form-label">Username</label>
                        <input type="text" class="form-control" id="username" name="username" placeholder="Enter username" required>
                    </div>
                    <div class="mb-3">
                        <label for="password" class="form-label">Password</label>
                        <input type="password" class="form-control" id="password" name="password" placeholder="Enter password" required>
                    </div>
                    <div class="mb-3">
                        <label for="role" class="form-label">Role</label>
                        <select class="form-select" id="role" name="role" required>
                            <option value="user" selected>User</option>
                        </select>
                    </div>
                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary btn-lg rounded-pill shadow-sm">
                            <i class="bi bi-person-plus"></i> Create User
                        </button>
                    </div>
                </form>
                <div class="mt-3 text-center">
                    <a href="/manage_user" class="btn btn-link">Back to Manage Users</a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>